{% extends 'base.html.twig' %}

{% block title %}Workbook{% endblock %}

{% block body %}
    <h1>Workbook</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ workbook.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ workbook.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('workbook_index') }}">back to list</a>

    <a href="{{ path('workbook_edit', {'id': workbook.id}) }}">edit</a>

    {{ include('workbook/_delete_form.html.twig') }}
{% endblock %}
