{% extends 'base.html.twig' %}

{% block title %}Leaf{% endblock %}

{% block body %}
    <h1>Leaf</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ leaf.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ leaf.Name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ leaf.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('leaf_index') }}">back to list</a>

    <a href="{{ path('leaf_edit', {'id': leaf.id}) }}">edit</a>

    {{ include('leaf/_delete_form.html.twig') }}
{% endblock %}
